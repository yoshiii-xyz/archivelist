[package]
name = "archivelist"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "List contents of tar, zip, and other archives"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
zip = "0.6"
tar = "0.4"
